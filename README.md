# 🎥 Telegram Stream Bot

Бот для керування RTMP трансляціями в Telegram через FFmpeg.

## 🚀 Швидкий старт

### 1. Створення бота в Telegram
1. Відкрийте [@BotFather](https://t.me/BotFather)
2. Надішліть `/newbot`
3. Оберіть назву та username для бота
4. Збережіть отриманий токен

### 2. Встановлення

```bash
# Клонування або завантаження файлів
cd stream-bot/

# Запуск скрипта встановлення
chmod +x install.sh
./install.sh
```

### 3. Налаштування

Відредагуйте файл `.env`:
```env
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
ADMIN_IDS=123456789
MAX_STREAMS_PER_USER=1
FFMPEG_PATH=ffmpeg
```

### 4. Запуск

```bash
# Активація віртуального середовища
source venv/bin/activate

# Запуск бота
python3 stream_bot_advanced.py
```

## 📋 Команди бота

### Базові команди
- `/start` - Початок роботи з ботом
- `/help` - Детальна допомога
- `/stream <URL> <RTMP_KEY>` - Запустити трансляцію
- `/stop` - Зупинити всі трансляції
- `/stop_id <ID>` - Зупинити конкретну трансляцію
- `/status` - Перевірити статус трансляцій

### Збережені конфігурації
- `/save <назва> <URL> <RTMP_KEY>` - Зберегти конфігурацію
- `/list` - Список збережених конфігурацій
- `/run <назва>` - Запустити збережену конфігурацію
- `/delete <назва>` - Видалити конфігурацію

### JSON конфігурація
- `/config` - Завантажити конфігурацію з JSON файлу

## 💡 Приклади використання

### Запуск простої трансляції
```
/stream https://example.com/stream.m3u8 1424308297:1gKBAQ5k0MYZTWw6w9HCkg
```

### Збереження та використання конфігурації
```
/save webcam https://cam.example.com/live.m3u8 1424308297:key
/run webcam
```

### JSON конфігурація
Створіть файл `config.json`:
```json
{
  "stream_url": "https://example.com/stream.m3u8",
  "rtmp_key": "1424308297:1gKBAQ5k0MYZTWw6w9HCkg",
  "name": "My Camera"
}
```
Надішліть файл з командою `/config`

## 🛠 Технічні деталі

### Параметри FFmpeg
- **Відео**: H.264, 1280x720, 25fps
- **Аудіо**: AAC, 128kbps, стерео
- **Колірний простір**: BT.709
- **Пресет**: veryfast (оптимальний баланс якості/швидкості)

### Підтримувані формати вхідних потоків
- HLS (.m3u8)
- RTMP
- HTTP/HTTPS стріми
- Локальні файли

## 🔧 Системний сервіс

Для автоматичного запуску бота при старті системи:

```bash
# Створення сервісу (виконується при встановленні)
sudo systemctl enable telegram-stream-bot

# Управління сервісом
sudo systemctl start telegram-stream-bot    # Запуск
sudo systemctl stop telegram-stream-bot     # Зупинка
sudo systemctl restart telegram-stream-bot  # Перезапуск
sudo systemctl status telegram-stream-bot   # Статус
```

## 📊 Моніторинг

Перегляд логів:
```bash
# Для сервісу
sudo journalctl -u telegram-stream-bot -f

# При ручному запуску
tail -f stream_bot.log
```

## ⚠️ Вирішення проблем

### FFmpeg не знайдено
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

### Помилка підключення до RTMP
- Перевірте правильність RTMP ключа
- Переконайтеся, що ключ активний в Telegram
- Перевірте мережеве з'єднання

### Висока затримка трансляції
- Використовуйте пресет `ultrafast` замість `veryfast`
- Зменшіть роздільну здатність до 960x540
- Перевірте швидкість інтернет-з'єднання

## 📈 Оптимізація

### Для низької затримки
```python
'-preset', 'ultrafast',
'-tune', 'zerolatency',
```

### Для кращої якості
```python
'-preset', 'slow',
'-crf', '23',
```

### Для економії трафіку
```python
'-b:v', '1000k',  # Обмеження бітрейту відео
'-b:a', '96k',     # Зменшення бітрейту аудіо
```

## 🔐 Безпека

1. **Ніколи не діліться токеном бота**
2. Використовуйте ADMIN_IDS для обмеження доступу
3. Регулярно оновлюйте залежності
4. Моніторте використання ресурсів

## 📝 Ліцензія

MIT License

## 🤝 Підтримка

При виникненні проблем:
1. Перевірте логи бота
2. Переконайтеся в правильності конфігурації
3. Перевірте версію FFmpeg: `ffmpeg -version`

## 🚦 Статуси процесів

- 🟢 **Активна** - трансляція працює
- 🔴 **Завершена** - трансляція зупинена
- ⚠️ **Помилка** - проблема з трансляцією

## 💰 Використання ресурсів

Приблизне споживання на одну трансляцію:
- **CPU**: 20-40% (залежить від пресету)
- **RAM**: 100-200 MB
- **Мережа**: 2-4 Mbps upload

## 🔄 Оновлення

```bash
cd stream-bot/
git pull  # якщо використовуєте git
source venv/bin/activate
pip install --upgrade -r requirements.txt
sudo systemctl restart telegram-stream-bot
```
